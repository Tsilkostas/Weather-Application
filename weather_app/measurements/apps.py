from django.apps import AppConfig


class MeasurementsConfig(AppConfig):
    """
    Configuration class for the 'measurements' Django application.
 
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'measurements'
